# tro-webpage-cli

Read web documentation through MCP **`tro`** (`read_url`, `read_urls`) — not raw HTML or browser fetch.

See `.claude/rules/docs.md` for tool usage. MCP config: `.mcp.json` (Claude Code) or `config/claude_desktop_config.example.json` (Claude Desktop).
