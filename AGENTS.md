# Agents

Read documentation with MCP **`tro`** — tools **`read_url`** (one page) or **`read_urls`** (parallel batch).

| Client | Config | Rules |
| ------ | ------ | ----- |
| Cursor | `.cursor/mcp.json` | `.cursor/rules/docs.mdc` |
| Claude Code | `.mcp.json` | `.claude/rules/docs.md` |
| Claude Desktop | `config/claude_desktop_config.example.json` | (merge into app config) |
