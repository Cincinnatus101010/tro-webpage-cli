{
  "mcpServers": {
    "tro": {
      "command": "cargo",
      "args": [
        "run",
        "--quiet",
        "--release",
        "--manifest-path",
        "${CLAUDE_PROJECT_DIR:-.}/Cargo.toml",
        "--bin",
        "tro-mcp"
      ]
    }
  }
}
